% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/inference.R
\name{combine_harm_draw_inference}
\alias{combine_harm_draw_inference}
\title{Combine Inference Across Bayesian Harmonization Draws}
\usage{
combine_harm_draw_inference(
  results,
  group_cols = c("feature", "term"),
  estimate_col = "estimate",
  se_col = "se",
  conf_level = 0.95
)
}
\arguments{
\item{results}{Data frame containing one row per posterior draw and inferential
target.}

\item{group_cols}{Character vector identifying the inferential target, such as
\code{c("feature", "term")}.}

\item{estimate_col}{Name of the estimate column. Defaults to \code{"estimate"}.}

\item{se_col}{Name of the conditional standard-error column. Defaults to
\code{"se"}.}

\item{conf_level}{Confidence level for normal-approximation intervals.
Defaults to \code{0.95}.}
}
\value{
A data frame containing posterior-averaged estimates, within-
harmonization variance, between-harmonization variance, total variance,
propagated standard errors, confidence intervals, and two-sided p-values.
}
\description{
Combines repeated downstream estimates obtained from posterior harmonized-
data draws using a law-of-total-variance decomposition. This is intended for
posterior Monte Carlo draws and therefore does not use the conventional
multiple-imputation \code{(1 + 1/S)} inflation factor.
}
