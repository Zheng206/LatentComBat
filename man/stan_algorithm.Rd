% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/stan_method.R
\name{stan_algorithm}
\alias{stan_algorithm}
\alias{stan_algorithm.univariate}
\alias{stan_algorithm.sva_combat}
\title{Run a Stan Harmonization Algorithm}
\usage{
stan_algorithm(type, ...)

\method{stan_algorithm}{univariate}(
  type,
  stan_data,
  chains = 3,
  parallel_chains = 3,
  batch_names,
  adapt_delta = 0.98,
  iter_sampling = 1000,
  iter_warmup = 1000,
  max_treedepth = 12,
  ...
)

\method{stan_algorithm}{sva_combat}(
  type,
  stan_data,
  chains = 3,
  parallel_chains = 3,
  posterior_draws = 0L,
  posterior_seed = 1L,
  adapt_delta = 0.98,
  iter_sampling = 1000,
  iter_warmup = 1000,
  max_treedepth = 12,
  ...
)
}
\arguments{
\item{type}{Object used for S3 method dispatch.}

\item{...}{Additional arguments reserved for method compatibility.}

\item{stan_data}{List returned by \code{\link[=stan_data_prep]{stan_data_prep()}} for the joint
SVA-ComBat model.}

\item{chains}{Number of MCMC chains. Defaults to \code{3}.}

\item{parallel_chains}{Number of chains run in parallel. Defaults to \code{3}.}

\item{batch_names}{Character vector containing the batch labels used to
assign row names to the returned batch-effect matrices.}

\item{adapt_delta}{Target acceptance probability for the NUTS sampler.
Larger values use smaller leapfrog step sizes and can reduce divergent
transitions at the cost of additional computation. Defaults to \code{0.98}.}

\item{iter_sampling}{Number of post-warmup MCMC iterations retained per
chain. Defaults to \code{1000}.}

\item{iter_warmup}{Number of warmup iterations per chain used for sampler
adaptation. Warmup draws are not retained for posterior inference.
Defaults to \code{1000}.}

\item{max_treedepth}{Maximum tree depth allowed for each NUTS trajectory.
Larger values permit longer trajectories when the posterior geometry is
difficult to explore, at the cost of additional computation. Defaults to
\code{12}.}

\item{posterior_draws}{Number of posterior draws of \code{R_adj} to retain for
uncertainty-aware downstream inference. Set to \code{0} to retain only the
posterior mean. Defaults to \code{0}.}

\item{posterior_seed}{Integer random seed used when subsampling retained
posterior draws. Defaults to \code{1}.}
}
\value{
The fitted Stan harmonization result.

A list containing:
\describe{
\item{gamma_star}{
Matrix of posterior mean additive batch-effect estimates, with batches
in rows and features in columns.
}
\item{delta_star}{
Matrix of posterior mean multiplicative batch-effect variance estimates,
with batches in rows and features in columns.
}
}

A list containing:
\describe{
\item{R_adj}{
Posterior mean of the batch-adjusted residual matrix, with dimensions
\verb{N x P}.
}
\item{R_adj_draws}{
Optional array of retained posterior draws of the adjusted residual
matrix, with dimensions \verb{S x N x P}, where \code{S} is the number of retained
draws. \code{NULL} when \code{posterior_draws = 0}.
}
\item{draw_ids}{
Integer indices identifying the posterior draws retained in
\code{R_adj_draws}. Empty when \code{posterior_draws = 0}.
}
\item{H_hat}{
Posterior mean estimate of the latent-factor scores. For
cross-sectional models this is the posterior mean of \code{H}; for
longitudinal models it is reconstructed from the subject-level and
observation-level latent components.
}
\item{latent_hat}{
Posterior mean of the feature-level latent contribution, with dimensions
\verb{N x P}.
}
\item{fit}{
The fitted \link[cmdstanr:CmdStanMCMC]{cmdstanr::CmdStanMCMC} object, retained for convergence
diagnostics and additional posterior summaries.
}
}
}
\description{
S3 generic for fitting Stan-based harmonization models.
}
\details{
MCMC sampling is performed using Stan's NUTS sampler. The \code{adapt_delta}
argument controls the target acceptance probability used during step-size
adaptation, while \code{max_treedepth} controls the maximum length of the NUTS
trajectory. Increasing either setting can improve sampling for difficult
posterior geometries, but may increase computation time.

The \code{iter_warmup} argument controls the number of warmup iterations used for
sampler adaptation, and \code{iter_sampling} controls the number of retained
post-warmup draws per chain.

Posterior draws of \code{R_adj} are only retained when \code{posterior_draws > 0},
which can substantially reduce memory usage when uncertainty propagation is
not required.
}
