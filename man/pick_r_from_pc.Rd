% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/covbat.R
\name{pick_r_from_pc}
\alias{pick_r_from_pc}
\title{Select the Number of Principal Components by Explained Variance}
\usage{
pick_r_from_pc(pca_fit, var_thresh = 0.95, min_r = 5, max_r = Inf)
}
\arguments{
\item{pca_fit}{A fitted PCA object containing a numeric \code{sdev} component,
such as an object returned by \code{\link[stats:prcomp]{stats::prcomp()}}. The squared values of
\code{sdev} are used to calculate the proportion of variance explained by
each principal component.}

\item{var_thresh}{Numeric scalar between 0 and 1 specifying the minimum
cumulative proportion of variance to be explained by the retained
principal components. Defaults to \code{0.95}.}

\item{min_r}{Integer specifying the minimum number of principal components
to retain. Defaults to \code{5}.}

\item{max_r}{Integer or \code{Inf} specifying the maximum number of principal
components to retain. Defaults to \code{Inf}.}
}
\value{
An integer giving the selected number of principal components.
The selected value is the smallest number of PCs whose cumulative
explained variance reaches \code{var_thresh}, constrained to be at least
\code{min_r}, no greater than \code{max_r}, and no greater than the total number
of available principal components.
}
\description{
Selects the smallest number of principal components (PCs) required to
explain at least a specified proportion of the total variance, subject to
user-defined lower and upper bounds on the number of retained components.
}
\details{
Let \eqn{d_k} denote the standard deviation of the \eqn{k}-th principal
component. The cumulative proportion of variance explained by the first
\eqn{r} components is

\deqn{
\frac{\sum_{k=1}^{r} d_k^2}
     {\sum_{k=1}^{K} d_k^2},
}

where \eqn{K} is the total number of available principal components.
The function first selects the smallest \eqn{r} for which this cumulative
proportion is at least \code{var_thresh}, and then applies the \code{min_r} and
\code{max_r} constraints.
}
\examples{
x <- scale(USArrests)
pca_fit <- prcomp(x)

pick_r_from_pc(
  pca_fit,
  var_thresh = 0.95,
  min_r = 2
)

pick_r_from_pc(
  pca_fit,
  var_thresh = 0.95,
  min_r = 2,
  max_r = 3
)

}
