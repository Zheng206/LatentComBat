% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bayesian_diagnostics.R
\name{bayes_diagnostics}
\alias{bayes_diagnostics}
\alias{print.latentcombat_bayes_diagnostics}
\alias{summary.latentcombat_bayes_diagnostics}
\alias{print.summary_latentcombat_bayes_diagnostics}
\title{Lightweight Diagnostics for Bayesian LatentComBat / Univariate Stan Fits}
\usage{
bayes_diagnostics(
  fit,
  variables = NULL,
  rhat_threshold = 1.01,
  ess_threshold = 100,
  ebfmi_threshold = 0.3,
  ebfmi_fail_threshold = 0.2,
  divergence_fail_prop = 0.01,
  treedepth_warning_prop = 0.01,
  treedepth_fail_prop = 0.05,
  max_treedepth = 12L
)

\method{print}{latentcombat_bayes_diagnostics}(x, ...)

\method{summary}{latentcombat_bayes_diagnostics}(object, ...)

\method{print}{summary_latentcombat_bayes_diagnostics}(x, ...)
}
\arguments{
\item{fit}{A fitted \code{latentcombat_bayesian} object, a univariate Stan result
containing a CmdStanR \code{CmdStanMCMC} object in \verb{$fit}, or a raw
\code{CmdStanMCMC} object.}

\item{variables}{Optional character vector of Stan base variable names to
monitor. If \code{NULL}, a lightweight model-aware default is used.}

\item{rhat_threshold}{R-hat warning threshold. Defaults to \code{1.01}.}

\item{ess_threshold}{Minimum bulk/tail ESS used as a warning threshold.
Defaults to \code{100}.}

\item{ebfmi_threshold}{E-BFMI warning threshold. Defaults to \code{0.30}.}

\item{ebfmi_fail_threshold}{E-BFMI failure threshold. Defaults to \code{0.20}.}

\item{divergence_fail_prop}{Proportion of post-warmup draws with divergences
above which sampler geometry is classified as \code{FAIL}. Defaults to \code{0.01}.}

\item{treedepth_warning_prop}{Proportion of post-warmup draws reaching the
maximum tree depth above which sampler geometry is classified as \code{WARNING}.
Defaults to \code{0.01}.}

\item{treedepth_fail_prop}{Proportion of post-warmup draws reaching the
maximum tree depth above which sampler geometry is classified as \code{FAIL}.
Defaults to \code{0.05}.}

\item{max_treedepth}{Maximum NUTS tree depth used when counting treedepth
hits. Defaults to \code{12}.}

\item{x}{An object returned by \code{\link[=bayes_diagnostics]{bayes_diagnostics()}}.}

\item{...}{Additional arguments passed to or from methods.}

\item{object}{An object returned by \code{\link[=bayes_diagnostics]{bayes_diagnostics()}}.}
}
\value{
An object of class \code{latentcombat_bayes_diagnostics}.
}
\description{
Evaluates the most important MCMC diagnostics for a Joint Bayesian
LatentComBat fit while deliberately excluding element-wise latent-factor
coordinates such as \code{H} and \code{Lambda} from the overall convergence decision.
Those coordinates can exhibit sign, permutation, or rotation ambiguity even
when the fitted latent surface is stable.
}
\details{
The diagnostic has two components:
\itemize{
\item sampler geometry: divergences, treedepth hits, and E-BFMI;
\item convergence of a small set of identifiable / structural parameter
families using R-hat and effective sample size.
}
}
