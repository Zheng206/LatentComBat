% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/com_harm.R
\name{com_harm}
\alias{com_harm}
\title{Harmonize Data Across Batches}
\usage{
com_harm(
  bat,
  data,
  covar,
  model = lm,
  formula = NULL,
  ref.batch = NULL,
  eb = TRUE,
  stan = FALSE,
  robust.LS = FALSE,
  cov = FALSE,
  var_thresh = 0.95,
  min_rblock = 1,
  max_rblock = Inf,
  sva = FALSE,
  bayes_sva = FALSE,
  method = NULL,
  K_max = 10,
  K_buffer = 2,
  subj = NULL,
  var_stable = FALSE,
  protected_covar = NULL,
  posterior_draws = 0L,
  posterior_seed = 1L,
  adapt_delta = 0.98,
  iter_sampling = 1000,
  iter_warmup = 1000,
  max_treedepth = 12,
  keep_inference_data = TRUE,
  ...
)
}
\arguments{
\item{bat}{Batch or site factor with one value per observation.}

\item{data}{Numeric matrix or data frame with observations in rows and
features in columns.}

\item{covar}{Optional data frame of observed covariates to preserve.}

\item{model}{Mean-model fitting function, such as \code{\link[stats:lm]{stats::lm()}} or
\code{\link[mgcv:gam]{mgcv::gam()}}.}

\item{formula}{Optional model formula.}

\item{ref.batch}{Optional reference batch.}

\item{eb}{Logical indicating whether empirical-Bayes shrinkage is used in
the sequential ComBat step. Defaults to \code{TRUE}.}

\item{stan}{Logical indicating whether Stan-based estimation is used in the
sequential workflow. Defaults to \code{FALSE}.}

\item{robust.LS}{Logical indicating whether robust location and scale
estimates are used. Defaults to \code{FALSE}.}

\item{cov}{Logical indicating whether covariance harmonization is applied.
Defaults to \code{FALSE}.}

\item{var_thresh}{Cumulative variance threshold used to select principal
components for covariance harmonization. Defaults to \code{0.95}.}

\item{min_rblock}{Minimum number of principal components to harmonize.
Defaults to \code{1}.}

\item{max_rblock}{Maximum number of principal components to harmonize.
Defaults to \code{Inf}.}

\item{sva}{Logical indicating whether surrogate-variable adjustment is
performed before ComBat in the sequential workflow. Defaults to \code{FALSE}.}

\item{bayes_sva}{Deprecated compatibility flag. If \code{TRUE} and \code{method} is
not specified, the Bayesian workflow is selected.}

\item{method}{Harmonization strategy, either \code{"sequential"} or \code{"bayesian"}.
If \code{NULL}, the sequential workflow is used unless \code{bayes_sva = TRUE}.}

\item{K_max}{Maximum number of latent factors considered in latent-variable
estimation. Defaults to \code{10}.}

\item{K_buffer}{Additional latent dimensions considered during latent-factor
selection. Defaults to \code{2}.}

\item{subj}{Optional subject identifier for longitudinal or repeated-measures
data.}

\item{var_stable}{Logical indicating whether latent variance stabilization
is applied before the final ComBat step. This option is independent of
\code{sva}. Defaults to \code{FALSE}.}

\item{protected_covar}{Optional additional covariates or protected subspace
used to reduce removal of preserved biological variation.}

\item{posterior_draws}{Number of posterior harmonized-data draws to retain
when \code{method = "bayesian"}. Defaults to \code{0}, which preserves the usual
point-harmonization workflow without storing draws.}

\item{posterior_seed}{Random seed used only to select retained posterior
draws from the fitted Bayesian model. Defaults to \code{1}.}

\item{adapt_delta}{Target acceptance probability for the NUTS sampler.
Larger values use smaller leapfrog step sizes and can reduce divergent
transitions at the cost of additional computation. Defaults to \code{0.98}.}

\item{iter_sampling}{Number of post-warmup MCMC iterations retained per
chain when \code{method = "bayesian"}. Defaults to \code{1000}.}

\item{iter_warmup}{Number of warmup iterations per chain used for NUTS
adaptation when \code{method = "bayesian"}. Warmup draws are not retained for
posterior inference. Defaults to \code{1000}.}

\item{max_treedepth}{Maximum tree depth allowed for each NUTS trajectory.
Larger values permit longer trajectories when the posterior geometry is
difficult to explore, at the cost of additional computation. Defaults to
\code{12}.}

\item{keep_inference_data}{Logical indicating whether the original inputs and
harmonization settings needed by \code{\link[=harm_inference]{harm_inference()}} should be stored in the
fitted object. Defaults to \code{TRUE}.}

\item{...}{Additional arguments passed to lower-level model-fitting and
harmonization functions.}
}
\value{
A harmonization result returned by either \code{\link[=com_harm.sequential]{com_harm.sequential()}}
or \code{\link[=com_harm.bayesian]{com_harm.bayesian()}}, depending on the selected method.
}
\description{
Main user-facing function for batch harmonization. Depending on \code{method},
the function applies either a sequential SVA/ComBat workflow or a joint
Bayesian latent ComBat model.
}
\details{
With \code{method = "sequential"}, optional surrogate-variable adjustment and
latent variance stabilization are performed before the final ComBat step.
When both \code{sva = TRUE} and \code{var_stable = TRUE}, latent mean adjustment is
performed first, followed by variance stabilization.

With \code{method = "bayesian"}, latent variation and batch effects are estimated
jointly using the Bayesian latent ComBat model.
}
